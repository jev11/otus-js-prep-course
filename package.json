{
  "name": "otus-js-prep-course",
  "version": "1.0.0",
  "description": "home assignment solution for Otus JS prep course",
  "private": true,
  "scripts": {
    "test": "jest",
    "lint": "prettier --check src/**/*.js && eslint src/**/*.js",
    "lint:fix": "prettier --write src/**/*.js && eslint --fix src/**/*.js",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@babel/core": "^7.13.16",
    "@babel/preset-env": "^7.13.15",
    "babel-jest": "^26.6.3",
    "eslint": "^7.25.0",
    "eslint-config-airbnb-base": "^14.2.1",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-import": "^2.22.1",
    "eslint-plugin-jest": "^24.3.6",
    "husky": ">=6",
    "jest": "^26.6.3",
    "lint-staged": ">=10",
    "prettier": "^2.2.1"
  },
  "lint-staged": {
    "*.js": "eslint --cache --fix",
    "*.{js,css,md}": "prettier --write"
  }
}
